# Los hooks corren en Git Bash: con CRLF bash falla en la primera linea.
# Git for Windows instala con core.autocrlf=true; esto lo pisa para este repo.
* text=auto
*.sh text eol=lf
*.json text eol=lf
*.md text eol=lf
